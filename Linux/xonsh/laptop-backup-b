#!/bin/sh
//bin/true; /opt/anaconda3/envs/dotnet/lib/dotnet/dotnet "$0" -- "$@"; __rc=$?; [ -t 1 ] && printf '\033[?1l\033>'; exit $__rc
#:package Spectre.Console@0.57.2
#:property Configuration=Release
#:include utilities.cs

// Mirror $HOME onto LAPTOP_BACKUP_B, keeping one previous generation.

ArgVals a = Sys.WithDryRun(new Args("laptop-backup-b", "Mirror $HOME onto LAPTOP_BACKUP_B")).Parse(args);
Sys.Apply(a);

Ui.Begin("Laptop Backup B...");

string root = Path.Combine(Paths.Media, "LAPTOP_BACKUP_B");
string current = Path.Combine(root, "@current");

Sys.Remove(Sys.CopyLog);
Sys.RotatePreviousSnapshot(current + "/", Path.Combine(root, "@previous"));

Ui.Info("Synchronizing...");
Sys.SudoRsync("-vahiP", "--delete",
              $"--log-file={Sys.CopyLog}",
              $"--exclude-from={Sys.HomeExcludes}",
              Paths.Home + "/", current + "/");

Sys.Stamp("laptop-backup-b");
Sys.StampFile(Path.Combine(current, "backup_date.txt"));

return Ui.Done();
